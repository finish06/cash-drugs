# Retrospective — drugs
**Period:** 2026-03-05 → 2026-03-07
**Context:** Mixed (2 away-mode sessions + active collaborative work)
**Mode:** Interactive

## Period Summary

- Specs completed: 1 (structured-logging)
- Commits: 16
- Agent learnings recorded: 0 (no JSON files existed — gap identified)
- Human directives captured: 5
- Away-mode sessions: 2

## Human Directives

| # | Directive | Scope | Source |
|---|-----------|-------|--------|
| 1 | Use `log/slog` for structured logging, not third-party loggers | project | Interview |
| 2 | INFO = "what code is running", DEBUG = "each step in the code" | workstation | Interview |
| 3 | Config-only approach for new API endpoints — no code changes needed for search | project | Conversation |
| 4 | Default log level warn in config.yaml, debug in docker-compose | project | Conversation |
| 5 | Cache warm-up should skip fetch when MongoDB has fresh data | project | Conversation |

**Confirmation:** Confirmed by human

## Agent Observations (Project)

| # | Observation | Severity |
|---|-------------|----------|
| 1 | DailyMed API uses `drug_name` (underscore) not `drugname` — discovered via live testing | medium |
| 2 | Cache warm-up was re-fetching all paginated endpoints on every restart | high |
| 3 | 5 of 6 specs still marked "Draft" despite features being fully implemented | medium |
| 4 | No learnings.json was created — checkpoint triggers never fired | high |

### ADD Methodology Adherence

| Rule | Status | Detail |
|------|--------|--------|
| Spec-before-code | Partial | structured-logging had full TDD cycle; drug search was config-only; cache freshness had no spec |
| TDD cycles | Partial | Structured logging followed RED-GREEN-REFACTOR; other features were iterative |
| Auto-handoffs | Fail | No .add/handoff.md written despite multiple commits and session transitions |
| Learning checkpoints | Fail | No .add/learnings.json exists — zero checkpoints at any trigger |
| Quality gates | Partial | go vet and go test run regularly; no formal /add:verify invocations until retro |
| Source control | Pass | All 16 commits use conventional format |

**Refinement:** Human confirmed observations as-is.

## What Went Well

- Final outcome works well — product is functional and reliable
- Config-driven architecture is very nice — enables new endpoints without code changes
- Speed of creation was fast
- Coverage at 81% with integration tests

## What Needs Improvement

- E2E testing against actual config to validate endpoints work (would have caught drugname param bug)
- Agent should proactively surface when ADD process steps need to run (verify, handoff, learnings)
- Specs need to be marked Complete when features are done
- Learnings checkpoints need to actually fire

## Scores

| Metric | Score | Evidence |
|--------|-------|----------|
| Human Collaboration | 8.0 / 9.0 | Fast iteration, clear directives, config-driven approach validated |
| ADD Methodology Effectiveness | 4.5 / 9.0 | 1/6 specs Complete, no learnings.json, no handoffs, no verify until retro |
| Swarm Effectiveness | 3.0 / 9.0 | No parallel agents, no swarm-state, context lost between sessions |

## ADD Methodology Feedback

- Agent should tell user when ADD commands need to run (e.g., verify, handoff) — don't silently skip process steps

## Agreed Changes

1. Create e2e tests that validate real config.yaml endpoints against DailyMed API
2. Agent will proactively call out when ADD process steps should be run
3. Mark completed specs as Complete
4. Write handoff.md after significant work

## Actions Taken

- Created .add/learnings.json with project learnings
- Created .add/observations.md
- Recorded retro scores
- Updated spec statuses
